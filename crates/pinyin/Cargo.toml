[package]
name = "pinyin"
version = "0.1.0"
edition = "2021"

[dependencies]
# Add dependencies here, for example:
# serde = { version = "1.0", features = ["derive"] }
# This is where you can specify dependencies for the pinyin crate.